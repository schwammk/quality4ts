{
  "name": "quality4ts",
  "version": "0.1.0",
  "description": "Combined quality dashboard: crap4ts + dry4ts + arch4ts",
  "type": "module",
  "bin": { "quality4ts": "dist/cli.js" },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "test": "vitest run"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "typescript": ">=5 <6"
  },
  "devDependencies": {
    "@types/node": "^20",
    "vitest": "^3"
  }
}
